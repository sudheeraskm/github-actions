name: First workflow
on: workflow_dispatch
jobs:
  first_job:
    runs_on: self-hosted
    steps:
      - name: print greeting
        run: echo "Hello world"
      - name: print good bye
        run: echo "good bye"
